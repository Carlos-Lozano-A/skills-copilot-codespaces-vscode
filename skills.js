function calculateNumbers(var1, var2) {
    const sumTotal = var1 + var2;
    const diffTotal = var1 - var2;
    const multTotal = var1 * var2;
    const divTotal = var1 / var2;

    return [sumTotal, diffTotal, multTotal, divTotal];
}